import {
  strDasherize
} from 'ember-ui/helpers/str-dasherize';

module('StrDasherizeHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = strDasherize(42);
  ok(result);
});
