import {
  camelizeWord
} from 'ember-ui/helpers/camelize-word';

module('CamelizeWordHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = camelizeWord(42);
  ok(result);
});
