import {
  placeholderValue
} from 'ember-ui/helpers/placeholder-value';

module('PlaceholderValueHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = placeholderValue(42);
  ok(result);
});
