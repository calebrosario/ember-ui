import {
  formatModelName
} from 'ember-ui/helpers/format-model-name';

module('FormatModelNameHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = formatModelName(42);
  ok(result);
});
