import getModelInfo from 'ember-ui/utils/get-model-info';

module('getModelInfo');

// Replace this with your real tests.
test('it works', function() {
  var result = getModelInfo();
  ok(result);
});
